#include <stdio.h>
#include <string.h>

/*Using the Puts() function*/

int main(void)
{
	char str1[52];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");

	puts(str1);
	return (0);
}
