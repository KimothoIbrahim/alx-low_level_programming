#include "main.h"
#include <stdio.h>
/**
 * main - func
 * @argc: desc
 * @argv: desc
 *
 * Return: always return 0
 */

int main(int __attribute__((__unused__))  argc, char *argv[])
{
	printf("%s \n", *argv);
	return (0);
}
